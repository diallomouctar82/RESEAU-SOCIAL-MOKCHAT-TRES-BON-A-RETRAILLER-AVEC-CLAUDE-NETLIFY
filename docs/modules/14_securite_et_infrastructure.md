# 🔒 MODULE 14 — SÉCURITÉ, RÔLES & INFRASTRUCTURE SYSTÈME
> **Contrôle d'Accès RBAC, Authentification, Protection des Données & Résilience d'Exécution**

---

## 🎯 1. VISION & OBJECTIF
- **Vision** : Garantir la souveraineté numérique de chaque citoyen de la plateforme, préserver la stricte confidentialité de ses données de vie et assurer une disponibilité sans faille du service.
- **Objectif** : Appliquer un contrôle d'accès basé sur les rôles (RBAC), sécuriser les clés d'API serveur, chiffrer les pièces sensibles et maintenir un journal d'audit complet.

---

## 👥 2. UTILISATEURS CONCERNÉS & PARCOURS
- **Publics** : Administrateurs système, superviseurs d'experts, utilisateurs généraux.
- **Parcours Type** :
  1. Authentification sécurisée via `components/Auth.tsx` (avec distinction automatique du compte superviseur `visionsmart224@gmail.com`).
  2. Attribution des droits, jetons de session et permissions d'interface.
  3. Journalisation transparente des événements de sécurité dans le Coffre-fort numérique (`SecurityLog`).

---

## ⚙️ 3. COMPOSANTS & ARCHITECTURE TECHNIQUE
- **Fichiers Clés** :
  - `components/Auth.tsx` : Écran de connexion (Google OAuth via Supabase Auth, permissions minimales).
  - `components/AdminDashboard.tsx` : Console d'administration et de supervision.
  - `components/admin/AdminStableVersionsTab.tsx` : onglet « Versions stables & restauration contrôlée » (DEC-2026-086) — dernières versions livrées (nom, date UTC, commit, PR, module, statut relatif à la version servie, preuves), version réellement servie par `moknet.net` (lecture sans cache de `/version.json` puis du bundle), ordre de restauration contrôlée (pré-contrôles, saisie exacte, motif, oranges reconnus, journal nominatif, procédure Netlify / Git, vérification après coup). Ne bascule pas la production : aucun secret côté navigateur.
  - `services/versions/` : `versionDuCode.ts` (la version que le code déclare, avancée par chaque mission qui livre du code), `stableVersions.ts` (registre des faits, identification, pré-contrôles, procédure, verdict — pur), `restaurationControlee.ts` (lecture réseau, journal des ordres : `platform_settings` clé `restauration_versions` + copie locale + audit).
  - `vite-plugins/versionJson.ts` : `version.json` émis au build (version, commit `COMMIT_REF`, `DEPLOY_ID`, branche, contexte, heure, bundle) et `__MOKNET_BUILD__` injecté (`vite.config.ts`).
  - `components/Settings.tsx` : Paramètres de sécurité, profil, 2FA et préférences.
  - `metadata.json` : Déclaration des permissions iframe et capacités serveur.
  - `services/supabaseClient.ts`, `services/auth.ts` : Client et flux d'authentification Supabase.
  - `services/health/healthRegistry.ts` : registre du tableau de bord « Santé Globale » (58 lignes, 12 domaines techniques de notation et 7 blocs de lecture — Sécurité, Application, Connecteurs, Live, VPS, Base de données, Services externes —, poids par domaine = 100 sous garde ; chaque ligne porte cause, impact, niveau de risque et une seule voie d'action : réparation automatique, guide manuel `manual` ou recommandation). `services/health/securityAudit.ts` : l'audit du 4/09/2026 (61 %) comme référence — 8 domaines, 14 constats, 3 vagues — et la note de sécurité vivante ; `services/health/healthGuide.ts` : résolution des liens `{supabase}`/`{repo}` des guides. `components/admin/HealthAssistant.tsx` + `services/health/assistant/repairCampaign.ts` et `assistantBrain.ts` (5/09/2026, DEC-2026-060) : l'Assistant IA de Santé Globale — moteur de campagne pur (portées tout / rouges / oranges / domaine / point, diagnostic → une confirmation → réparation point par point, arrêt « incontrôlable », restauration du lot) et cerveau (narration, intentions déterministes, contexte compact pour la passerelle IA), branchés sur le pipeline `health-guardian` et sur le moteur vocal partagé.
  - `supabase/functions/health-guardian/` : fonction Edge de sondage (`index.ts` seul au réseau, `evaluate.ts`, `liveTransportProbe.ts` et `liveEmergency.ts` purs), artefact déployé généré par `build-bundle.sh` (v3 le 5/09/2026 à 07:38 UTC : sondes CORS des six fonctions, façade HTTPS et porte `/rtc` du VPS, compteurs `super_admin`/`admin` ; repli CORS jamais `*` ; **v4 le 5/09/2026 à 10:49 UTC** : les trois actions de secours du direct, 66 588 octets, source relue identique octet pour octet au dépôt, retour arrière v3 vérifié avant le déploiement).
- **Modèles de Données (`types.ts` + Supabase `public.profiles`)** :
  - `UserRole` (`user | admin | expert | mentor | moderator | organization | super_admin`), `UserProfile`, `SecurityLog`, `DeviceSession`.

---

## 🛡️ 4. RÈGLES MÉTIER & SÉCURITÉ
- **Protection des Secrets Serveur** : Clés Gemini et API tierces isolées côté serveur, jamais transmises au navigateur. `service_role` Supabase jamais exposée au frontend (uniquement la clé `publishable`).
- **Principe du Moindre Privilège** : Les fonctions d'administration et de modération globale sont réservées au rôle `admin`, fixé **côté base** (trigger serveur), plus jamais calculé côté client. Row Level Security activé sur toutes les tables (voir `docs/SUPABASE_ARCHITECTURE.md`).
- **Conformité RGPD** : Droit à l'oubli, exportation complète des données (`cloud.ts`) et chiffrement local.

---

## 📊 5. ÉTAT DE DÉVELOPPEMENT & ÉVOLUTIONS
- **Terminé** : Authentification Supabase (Google OAuth), gestion des rôles server-side avec RLS, tableau de bord admin. **Tableau de bord « Santé Globale » (Super-Admin, 4 septembre 2026)** : 41 lignes évaluées côté serveur, aucun vert non mesuré (un évaluateur qui lève = blanc), réparation avec sauvegarde, journal et restauration ; **SAT-4 (5 septembre 2026, DEC-2026-054)** : la ligne « un direct peut réellement démarrer » interroge `ListRooms` avec la clé du coffre au lieu d'un ping — fonction Edge `health-guardian` v2 en production et démontrée (vert, 400 ms), ligne côté écran en production (PR #77, 5 septembre 2026). *(27 août 2026 : migration complète depuis l'ancienne session `localStorage` falsifiable — voir `docs/AUTHENTICATION.md`.)*
- **Terminé et en production contrôlée (5 septembre 2026, DEC-2026-055, v6.20.0)** : **SAT-5** — relance automatique bornée d'un direct gardée par l'état réel en base, écran « Ce direct est terminé. » sur un direct clos, et clôture horaire des directs zombies par `pg_cron` (première exécution réelle : 13 fermés, tracés dans `audit_logs`) ; prouvé au banc réel 39/39 contre un LiveKit vivant ; la frontière VPS (conteneur, clé, ports, version) reste à SAT-6/ACT.
- **Terminé et en production contrôlée (5 septembre 2026, DEC-2026-057, v6.22.0, PR #86)** : **Santé Globale v2** — santé et sécurité en % (audit du 4/09 intégré : 61 % de référence, 51 % mesuré sur 84 % le 5/09), graphiques et progression des vagues P0/P1/P2, sept domaines × rouge/orange/vert, fiche problème/cause/impact/risque/action sur 58 lignes, action manuelle guidée (endroit exact, lien, étapes) seulement quand rien d'automatique n'existe, jamais de faux bouton ; sondes nouvelles : CORS réel des fonctions (5/6 répondent `*`), VPS (façade + `/rtc/validate`), rang Admin Général (migration `20260905090000`, aucun droit ni donnée touchés).
- **Fonction Edge v4 DÉPLOYÉE ET VÉRIFIÉE EN PRODUCTION, client FUSIONNÉ, EN PRODUCTION CONTRÔLÉE (5 septembre 2026, DEC-2026-059, v6.24.0, PR #88 → `4486cc4`)** : **SAT-6** — le bouton de secours du direct, réservé à l'Admin Général : relancer la room d'un direct ou le clore depuis la Santé Globale, sans SSH ; rang relu **en base** (`health_my_rank`) avant toute lecture et de nouveau au geste, diagnostic qui compte les présents réels, confirmation signée (HMAC, cinq minutes, liée au geste, au direct et à l'acteur), re-mesure avant verdict, journal `audit_logs` (`health.emergency`) ; la clôture passe par la RLS avec l'identité de l'appelant, jamais par le rôle service ; les gestes SSH (conteneur, clé, ports UDP, version) sont listés comme action humaine. Greffé sur la Santé Globale v2 (panneau « Secours du direct » avant le journal, entrées « Secours » dans le journal). Le banc réel a aussi fait corriger deux défauts pré-existants : la fiche problème et la modale de réparation étaient cadrées sur l'onglet et non sur la fenêtre — un `transform` identité laissé par l'animation d'entrée piège tout `position:fixed`, désormais rendues par portail — et un spectateur pouvait être éjecté à son arrivée par une course du roster du direct. Mise en production contrôlée validée par la Direction le 5/09 (10h40 UTC) et menée dans l'ordre imposé : retour arrière v3 vérifié identique à l'ancienne production, fonction Edge v4 déployée à 10:49:57 UTC (source relue identique octet pour octet), backend vérifié avec trois vraies sessions (`probe` 45 lignes comme la v3, 403 `user`, lecture seule `admin`, gestes `super_admin`), geste réel « Clore » joué sur un direct de banc privé avec ligne `audit_logs` réelle ; client : PR #88 en fusion, bundle servi et contrôle du tableau de bord réel consignés ensuite. Le compte de la Direction est `admin` : lecture seule (« Diagnostic seulement ») tant qu'il ne porte pas le rang `super_admin`.
- **Terminé et en production contrôlée (5 septembre 2026, DEC-2026-060, v6.25.0, PR #91 → `main` `b36318b`, `moknet.net` sert `index-JAHL7yTa.js`)** : **Assistant IA de Santé Globale** — vocal et texte, avatar existant ; Analyser avec progression réelle et bilan dit ; Réparer tout le lot / les rouges seuls / les oranges seuls / un domaine / un point avec une seule confirmation par lot et le pourcentage à chaque boucle ; échec → cause, étapes exactes, lien ; arrêt immédiat si incontrôlable et retour à l'état stable (restauration du lot) ; « Diagnostiquer » quand le rang ne permet pas d'appliquer. Aucune table, migration ni fonction Edge nouvelle ; pas de vidéo HeyGen (passerelle synchrone). Ajustement v6.40.1 (PR #104) : champ de dialogue placé juste sous la conversation, rien à activer.
- **Développé, testé au banc, en préparation (5 septembre 2026, DEC-2026-086, v6.45.0)** : **Versions stables & restauration contrôlée** — onglet Super-Admin dédié : au moins les trois dernières versions stables avec nom, date, commit, PR, module, statut de production et preuves (registre relevé dans la mémoire vivante, vérifié par test) ; version réellement servie lue sur le réseau ; ordre de restauration jamais à l'aveugle (sept pré-contrôles, un rouge refuse, un orange exige une reconnaissance ; saisie exacte de la version ; motif ; journal partagé et audit ; procédure Netlify « Publish deploy » ou revert Git ; vérification après coup). Limite dite : la publication reste un geste manuel — un exécuteur serveur (jeton Netlify dans le Vault) est possible plus tard, sur décision de la Direction. Statut de production : voir DEC-2026-086.
- **Partiel / En cours** : Authentification biométrique WebAuthn / Passkeys ; email/mot de passe réel (reporté, Google restait prioritaire).
- **Évolutions Prévues** : Détection proactive des anomalies de connexion et alertes par notification push chiffrée ; activation de la protection "mots de passe compromis" de Supabase Auth une fois l'email/mot de passe implémenté.
