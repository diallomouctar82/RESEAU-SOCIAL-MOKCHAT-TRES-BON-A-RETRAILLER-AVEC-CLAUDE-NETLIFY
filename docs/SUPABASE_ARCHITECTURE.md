# 🗄️ ARCHITECTURE SUPABASE — LE MONDE À VOUS

> Socle backend (base de données, authentification, permissions, stockage, temps réel).
> Projet Supabase : **"monde a vous"** (`rqciahtpixdjbyoajomg`, région `eu-west-1`, Postgres 17).
> *Mise à jour : 27 août 2026 — migration complète Firebase → Supabase.*

---

## 1. Principe directeur

Toute l'app converge vers **une identité unique** : `auth.users` (Supabase Auth) + `public.profiles` (profil applicatif, 1:1). Chaque module métier référence `profiles.id`, jamais un identifiant local par module. RLS activé sur **toutes** les tables contenant des données utilisateur.

**Périmètre de cette mission** (décision explicite, voir section 4) : Identity, Social, Messagerie, Dossiers de vie, Carrière (scope réel), Éducation/Campus, Commerce minimal, Finance, Notifications, Fichiers, Live (intégral), catalogue Agents. **Hors périmètre** : Trade/Commerce Mondial (RFQ, CommercialDossier, salons, MokTrust — ~90 types), Tribus/Cercles riches — 0% de persistance prouvée dans le code au moment de la migration, laissés inchangés (écrans de démo) jusqu'à preuve de besoin produit réel.

---

## 2. Domaines et tables

| Domaine | Tables | Notes |
|---|---|---|
| **Identity** | `profiles`, `profile_skills`, `profile_badges` | `profiles.id` = `auth.users.id`. `role`/`credits`/`xp`/`level`/`next_level_xp` protégés en écriture (trigger, voir §5). `profile_skills`/`profile_badges` : lecture réelle (`services/profile.ts`), édition non exposée côté UI ; `source_type`/`source_id` (ajoutés 29 août 2026) préparent un enrichissement automatique futur (formation, certification, projet, recommandation, activité) — non implémenté, RLS reste privée `own_or_admin`. **LOOP 09/17 (mission Architecte MOCnet, découverte critique en testant la persistance réelle du nouveau réglage `notificationsMuted`, 29 août 2026)** — `public.profiles` n'avait **jamais** eu ni le `GRANT UPDATE` pour `authenticated`, ni la moindre policy RLS couvrant `UPDATE` (seule `profiles_select_visible`, lecture, existait) : vérifié avec un vrai jeton JWT contre PostgREST, **toute** tentative de sauvegarde de profil par **tout** utilisateur réel, depuis toujours, échouait avec `42501 permission denied for table profiles` avant même l'évaluation de RLS — masqué depuis l'origine car `handle_new_user()` (`SECURITY DEFINER`) n'a jamais eu besoin d'une policy INSERT, donc les profils apparaissaient bien à l'inscription, seule leur *modification* échouait silencieusement (erreur avalée par un `catch` côté client). Corrigé : `grant update on profiles to authenticated` + nouvelle policy `profiles_update_own` (`auth.uid() = id OR is_admin()`, même forme que `profiles_select_visible`) ; `UPDATE` retiré d'`anon` (grant résiduel sans effet, jamais légitime). Revérifié avec de vrais jetons JWT : Alice modifie son propre profil (bio + `privacy_settings.notificationsMuted`) → `200`, Bob tente de modifier celui d'Alice → `0` ligne renvoyée, Bob s'auto-attribue `role='super_admin'`/999999 crédits sur son PROPRE profil → neutralisé par le trigger déjà existant `protect_profile_sensitive_columns` (comportement inchangé, aucune régression). |
| **Social** | `posts`, `post_documents`, `comments`, `post_reactions`, `stories` | Réactions UNIQUE(post_id, user_id). Commentaires self-FK (réponses imbriquées). `post_documents` et `stories` existaient et étaient RLS depuis le début du projet mais n'étaient jamais consommés côté client (état React local uniquement) — branchés réellement le 29 août 2026 (LOOP 01/17, mission Architecte MOCnet, moteur de contenu unifié) : `services/supabaseClient.ts::getStories/createStory/createPostDocument`. `posts` a reçu à cette occasion `video_url`, `audio_url`, `status` (`draft`/`published`/`scheduled`/`archived` — un brouillon ou une publication programmée n'est **jamais** visible par un tiers quel que soit `visibility`, policy `posts_select_visible` corrigée en conséquence), `scheduled_at` (bascule automatique non encore implémentée, voir Tâches/Planification), `format` (propriété du contenu — texte/image/vidéo/audio/document/live_extract/composite ; `story` volontairement exclu, `stories` reste une table séparée à cycle de vie éphémère propre) et `source_type`/`source_id` (provenance polymorphe, ex. extrait de Live — non encore consommé). Upload réel vers le bucket Storage `public` (voir §6) pour image/vidéo/document de post et image de story, remplaçant le pattern base64/blob-URL historique. **LOOP 02/17 (gouvernance du contenu)** : suppression réelle (`deletePost`, confirmation requise côté UI, RLS `posts_delete_own_or_admin` déjà correcte), archivage réel via `status='archived'` (`updatePostStatus`, sort immédiatement du fil), partage réel via la fonction `increment_post_shares` (`SECURITY DEFINER` volontaire pour permettre à tout lecteur — pas seulement l'auteur — de partager un post, mais avec vérification de droits explicite dans la fonction : refuse tout post qui n'est pas simultanément `status='published'` et `visibility='public'` ; `EXECUTE` révoqué pour `anon`, réservé à `authenticated`). **Explicitement différé** (spécification trop large pour ce lot, à traiter comme un incrément dédié futur) : co-création multi-auteurs avec rôles/attribution (aucune table `post_collaborators` n'existe), pipeline de modération spécifique aux posts (report → revue admin → action) — la capacité technique de base (un admin peut déjà changer le `status` de n'importe quel post via `posts_update_own_or_admin`) existe, mais aucune UI de signalement/file de revue n'est construite. **LOOP 03/17 (intelligence & Architecte, aucune migration nécessaire — travail 100% couche applicative)** : création vocale du contenu via `services/content/contentVoiceCommands.ts::interpretContentVoiceCommand` (même patron que `services/live/liveVoiceCommands.ts` — registre `CONTENT_VOICE_CAPABILITIES` auto-descriptif, interprétation LLM via `generateJSON`, exécution 100% déterministe côté client dans `SocialFeed.tsx::dispatchContentVoiceAction`, jamais de logique dupliquée entre bouton tactile et commande vocale ; testé avec 3 appels réels authentifiés contre `ai-gateway` : SET_CONTENT, ASK_CLARIFICATION, SHORTEN — dégradation gracieuse vers `UNKNOWN` si l'IA échoue, jamais un blocage de la création manuelle). Connexion réelle Live→Contenu : `SocialLive.tsx::handleEndLive` génère un résumé IA à partir des vrais messages du Live (jamais de contenu inventé, repli honnête si rien à résumer) et `handlePublishLiveSummaryToFeed` l'enregistre comme brouillon réel (`status='draft'`, `format='live_extract'`, `source_type='live_session'`, `source_id`=id de session réel) — `LivePostContinuityModal.tsx` affiche désormais ce résumé réel (avec avertissement explicite quand seul l'exemple illustratif pré-existant est disponible) plutôt que le gabarit fictif d'origine. Correctif de robustesse au passage (régression introduite puis corrigée dans la même LOOP, avant tout commit) : un échec d'upload réel n'est plus jamais silencieusement remplacé par une URL `blob:` morte persistée en base — la publication est annulée avec un message explicite ; le mode hors-ligne/démo (sans Supabase configuré) repasse sur un encodage base64 auto-suffisant (`toDataUrl`) au lieu d'un `blob:` qui ne survit pas au rechargement. Recommandation/recherche de contenu et connexions Contenu↔Tribu/Classroom : non traitées dans cette LOOP (hors du sous-ensemble retenu), restent à planifier dans un futur incrément de ce moteur. |
| **Réseau social / relations** | `friendships`, `follows`, `user_blocks` | `friendships` (amitié réciproque, `pending`/`accepted`, cycle demande→acceptation) existe depuis la mission précédente (chantiers 3→2→1) mais sans anti-doublon ni séparation du modèle d'abonnement — corrigé au LOOP 04/17 (mission Architecte MOCnet, moteur social) : index unique `friendships_unique_pair` sur `(least(requester_id,addressee_id), greatest(...))` empêchant toute paire de demandes en double quel que soit le sens ou le statut (un second appel identique — double clic, retry — devient un no-op idempotent côté client, capturé sur l'erreur Postgres `23505`). **`follows` (nouvelle table)** : modèle unilatéral réellement distinct de l'amitié — jusqu'ici `MemberProfile.isFollowing` n'était qu'un alias de `friendshipStatus === 'friends'`, jamais un vrai abonnement ; désormais une vraie relation propre, RLS restreinte à `follower_id = auth.uid() OR followee_id = auth.uid()`, `profiles.followers_count`/`following_count` (colonnes déjà présentes, jamais alimentées) tenues à jour par le trigger `handle_follow_change`. **`user_blocks` (nouvelle table)** : blocage réel, distinct du signalement (qui reste un sujet de modération, non traité ici) — RLS strictement limitée à `blocker_id = auth.uid()` en lecture/écriture, de sorte que la personne bloquée ne peut jamais découvrir le blocage par une lecture directe (vérifié par test réel : 0 ligne visible côté bloqué). `blockUser()` met fin dans le même geste explicite (jamais une automatisation cachée) à toute amitié/abonnement existant entre les deux comptes. Deux fonctions `SECURITY DEFINER` (même patron que `increment_post_shares`, LOOP 02/17, `EXECUTE` révoqué pour `anon`) : `are_users_blocked(uuid,uuid)` (existence pure, jamais la ligne elle-même) et `can_send_friend_request(uuid,uuid)` (combine blocage + préférence `privacy_settings->>'allowFriendRequestsFrom'` du destinataire) — appelées directement dans les policies `INSERT` de `friendships` et `follows`, donc appliquées même à un appel direct de l'API REST, pas seulement via `services/supabaseClient.ts`. `privacySettings` (type client, `UserProfile`/`MemberProfile`) étend le JSON déjà existant sur `profiles` avec `allowFriendRequestsFrom` ('all'\|'none' — 'network' délibérément non proposé, nécessiterait une notion de relations mutuelles hors périmètre fondation), `showFollowersList`, `showFollowingList` (gate l'affichage des compteurs `followersCount`/`followingCount` à un tiers dans `MemberProfileModal.tsx`, jamais à soi-même). Vérifié par test réel (3 comptes éphémères, impersonation RLS via `set_config('request.jwt.claim.sub', ...)` + `set local role authenticated`) : anti-doublon (2 sens), refus de blocage invisible à la victime, refus de demande vers un profil `allowFriendRequestsFrom='none'`, compteurs `follows` en incrémentation/décrémentation réelle — tous les cas testés avant tout commit, données de test entièrement supprimées après. **LOOP 05/17 (découverte & Architecte)** : `searchProfiles(query?)` filtre réellement par nom/titre (`ilike`, sans repli accent-insensible — une recherche sans accents peut manquer un nom accentué, limitation connue non corrigée dans cette LOOP) — absent de `query`, comportement historique inchangé (liste pour le fil). Recommandation explicable sans fuite : `get_mutual_friends_count(uuid,uuid)` (`SECURITY DEFINER`, même patron que `are_users_blocked` — un entier seul, jamais l'identité des amis communs, nécessaire car la RLS de `friendships` empêche de lire le graphe d'un tiers), affichée comme badge « X amis en commun » dans le widget Suggestions (bornée à 4 appels). **Architecte — navigateur social** (`services/social/socialVoiceCommands.ts`, registre `SOCIAL_VOICE_CAPABILITIES`, même patron que `CONTENT_VOICE_CAPABILITIES`/`LIVE_VOICE_CAPABILITIES`) : commandes vocales pour envoyer/accepter/refuser une demande, retirer un ami, suivre/ne plus suivre, bloquer/débloquer, chercher des personnes — le LLM extrait uniquement l'intention et le nom tel qu'énoncé (jamais l'action elle-même), la résolution vers un membre réel et la levée d'ambiguïté (0 ou plusieurs correspondances → clarification, jamais une action sur la mauvaise personne) sont du code déterministe dans `SocialFeed.tsx::dispatchSocialVoiceAction`, qui réutilise mot pour mot les fonctions déjà testées du LOOP 04/17 (BLOCK en hérite donc la confirmation explicite, jamais de bypass vocal). Un seul moteur vocal partagé (`useVoiceAssistant`) route désormais vers l'interprète contenu ou social selon le dernier bouton micro pressé (`voiceIntentScopeRef`) plutôt que deux instances de reconnaissance concurrentes. Testé avec 4 appels réels authentifiés contre `ai-gateway` (SEND_FRIEND_REQUEST, ASK_CLARIFICATION, BLOCK, SEARCH_PEOPLE) — tous corrects, aucune invention de nom. **Explicitement différé** (spécification bien plus large que ce lot, à traiter dans un incrément dédié) : cercles manuels, rôles contextuels (collègue/camarade Classroom/etc.), présence/disponibilité, exclusion des utilisateurs bloqués des surfaces de découverte (le blocage n'est reflété que là où le membre est déjà affiché — widget Suggestions + fiche profil — pas encore un filtre actif sur la recherche/découverte elle-même), anti-abus avancé (quotas/anti-scraping — l'anti-doublon et le blocage du LOOP 04/17 restent la seule protection actuelle), historique relationnel structuré (`refusée`/`annulée` restent une suppression de ligne sans trace). |
| **Messagerie** | `conversations`, `conversation_participants`, `messages`, `agent_chat_sessions`, `agent_chat_messages` | `messages` en Realtime. Chat Expert IA enfin persisté (service `memoryService` existait côté code mais n'était jamais branché). **LOOP 06/17 (mission Architecte MOCnet, moteur de messagerie : fondation, 29 août 2026)** — découverte majeure en auditant le schéma réel avant d'écrire du code : `components/MoocChatFloating.tsx` et `services/supabaseClient.ts` étaient bâtis contre un schéma hypothétique jamais existant (`text`, `sender_name`, `media_url`, `voice_url`, `reply_to`, `reactions` au niveau racine, `participant_one_id`/`participant_two_id` sur `conversations`...) — **tout envoi de message réel échouait silencieusement depuis toujours** (capturé par un `.catch` muet), `getConversationsForUser` retournait `[]`, l'UI ne vivait que de `MOCK_CHATS`. Réécriture complète sur les vraies colonnes (`content`, `attachment_url`, `message_type`, `metadata.reactions`, `reply_to_id`, `client_message_id`, `direct_key`, `last_read_at`...), toutes déjà présentes en base et jamais consommées (même motif que `stories`/`post_documents` au LOOP 01/17). Trois correctifs de sécurité/robustesse réels trouvés et corrigés au passage : (1) `conversation_participants_insert_if_member_or_self` comparait `cp2.conversation_id` à lui-même — tautologie toujours vraie permettant à n'importe quel membre d'UNE conversation d'ajouter n'importe qui à n'importe QUELLE AUTRE conversation ; corrigée par une vraie corrélation à la ligne insérée + vérification de blocage. (2) `INSERT ... RETURNING` sur `conversations` échouait systématiquement (la policy `SELECT` exige déjà l'appartenance, or l'auto-enrôlement du créateur — trigger `enroll_creator_as_participant`, `SECURITY DEFINER` — n'est pas encore visible au moment où `RETURNING` est évalué : un vrai problème d'œuf-et-poule) ; corrigé en générant l'id de conversation côté client (`crypto.randomUUID()`) et en n'utilisant jamais `.select()` sur cet insert. (3) `enroll_creator_as_participant()` était exécutable par `anon` **et** `authenticated` en RPC direct (même défaut de grant par défaut que rencontré aux LOOP 02/17 et 04/17) — retiré ; le déclenchement par trigger ne dépend pas de ce GRANT (vérifié par re-test après coup : le trigger enrôle toujours correctement le créateur). Réactions rendues atomiques via `toggle_message_reaction(uuid,text)` (`SECURITY DEFINER`, verrou de ligne `FOR UPDATE`, vérification d'appartenance explicite — remplace un lire-modifier-écrire côté client sujet à une condition de course). Blocage réellement appliqué à la messagerie : `messages_insert_if_participant` refuse tout message dans une conversation 1-1 si l'une des deux parties a bloqué l'autre (réutilise `are_users_blocked` du LOOP 04/17, jamais un second mécanisme). Anti-doublon d'envoi réel via `client_message_id` (uuid, index unique `uq_message_client_id` sur `(sender_id, client_message_id)`, préexistant et jamais utilisé) — un retry réseau ou double clic percute `23505`, traité comme un no-op côté client plutôt qu'une erreur. Suppression = soft-delete honnête (`deleted_at` + `content=''`, jamais un `DELETE` qui effacerait l'historique pour l'autre partie). Le faux "message de bienvenue" fabriqué au nom du destinataire (hallucination pré-existante dans `handleStartDirectChat`, jamais réellement envoyée par cette personne) a été supprimé — une conversation neuve démarre vide. Pont réel `SocialFeed.tsx` (bouton "Message"/"Mooc Chat", vrai membre Supabase) → `MoocChatFloating.tsx` ajouté (`pendingDirectChatMember` remonté `App.tsx`→`Layout.tsx`, même patron que `isGoalModalOpen`) : **avant ce correctif, `onOpenDirectChat` n'avait aucun appelant réel** (`App.tsx` rendait `<SocialFeed onOpenLive={...} />` sans lui) — démarrer une première conversation avec une vraie personne n'avait donc aucun chemin UI fonctionnel, quel que soit l'état du moteur de messagerie lui-même. **Découverte critique faite en testant ce pont de bout en bout (2 comptes éphémères réels)** : le correctif `profiles_select_visible` (segment précédent de ce même LOOP) exige `profileVisibility='public'` ou une amitié acceptée pour voir le profil d'un tiers — or deux personnes qui démarrent une première conversation ne sont typiquement pas amies, et `'network'` (qui exige cette amitié) est le défaut de tout nouveau compte. Vérifié empiriquement : le nom/avatar de l'autre participant disparaissait silencieusement dans `conversation_participants(...profiles(...))`, l'UI serait tombée sur un générique "Membre" dans le cas le plus courant. Corrigé par `get_my_conversation_participant_profiles()` (`SECURITY DEFINER`, `EXECUTE` réservé à `authenticated` dès l'origine) — n'expose que `name`/`avatar_url`/`title`/`role` pour les membres d'une conversation partagée avec l'appelant, jamais la ligne `profiles` complète (email/téléphone/crédits/permissions/admin_notes) : la même divulgation minimale qu'une app de messagerie grand public révèle en ouvrant une conversation, pas plus — une politique plus large (assouplir `profiles_select_visible` elle-même) aurait exposé ces champs sensibles à quiconque prétend vouloir parler à quelqu'un, puisque `conversation_participants_insert_if_member_or_self` permet déjà d'ajouter un tiers à une conversation sans son consentement préalable. Vérifié de bout en bout via REST réel (pas seulement SQL simulé) : connexion `POST /auth/v1/token`, appel RPC et `select` PostgREST identiques à ceux du navigateur, avec le token réel de l'un des deux comptes de test. **Explicitement différé** (périmètre bien plus large que cette fondation, prévu ailleurs) : upload Storage réel des pièces jointes de chat (reste en base64 à travers la colonne `attachment_url`, réelle et persistée — une amélioration sur l'absence totale de persistance antérieure, mais pas l'état cible), résolution de réponse (`reply_to_id`) au-delà des messages déjà chargés en mémoire, UI d'édition de message (colonne `edited_at` prête, non exposée), affichage visuel de l'état "lu" (écriture réelle de `conversation_participants.last_read_at` via `markConversationRead`, comparaison/rendu non implémentés dans `ChatMessageItem.tsx`), permissions unifiées (`allowMessagesFrom` — qui peut m'écrire — colonne réelle jamais encore appliquée), traduction/résumé/IA de rédaction, indicateurs de frappe (code mort laissé tel quel) — tous prévus au LOOP 07/17 (Messagerie : intelligence & permissions) ou au-delà. **LOOP 07/17 (intelligence & permissions)** : `allowMessagesFrom` (colonne réelle depuis toujours, jamais vérifiée dans aucune policy — confirmé par lecture directe de `pg_policies`/`pg_proc`) enfin appliquée via `can_message_user(uuid,uuid)` (`SECURITY DEFINER`, même patron que `can_send_friend_request`) : combine blocage + préférence du destinataire, avec la définition RÉELLE déjà affichée dans `MemberProfileModal.tsx` ("mes abonnés et contacts réciproques" = `follows` OU `friendships` acceptée, PAS une simple amitié comme une première version de cette fonction l'avait supposé à tort avant tout test) — vérifié par 5 scénarios réels (`none` bloque, `network`+amitié passe, `network`+inconnu bloque, `network`+abonnement passe). Appliquée à l'INSERT sur `conversation_participants` ; `createDirectConversation` fait désormais un appel de pré-vérification (`error.code='MESSAGING_NOT_ALLOWED'`) AVANT toute écriture — nécessaire car `conversations` n'a aucune policy DELETE, un refus après coup aurait laissé une conversation orpheline impossible à nettoyer côté client. `MoocChatFloating.tsx` affiche désormais un message honnête sur ce refus au lieu de retomber silencieusement sur une fausse conversation locale qui n'aurait jamais pu délivrer de message. Nouveau service `services/messaging/messagingIntelligence.ts` (même patron que `handleEndLive` — anti-hallucination, dégradation gracieuse) : résumé de conversation (bouton dans l'en-tête), traduction de message avec langue d'origine toujours conservée (jamais un remplacement silencieux, action par message), assistance de rédaction qui corrige uniquement la forme du brouillon (jamais le fond, jamais un envoi automatique) — les 3 testées avec des appels réels authentifiés contre `ai-gateway`. **Correctif de confiance utilisateur, découvert en auditant cette LOOP** : l'UI de messagerie affichait activement, à tous les utilisateurs réels, deux bannières "chiffrés de bout-en-bout" et une "empreinte de chiffrement SHA256-AES" (`ChatMemberInfoModal.tsx`) — **aucune n'était vraie** (`content` stocké en clair, aucune bibliothèque E2E dans le dépôt, la chaîne SHA256-AES était une constante fixe jamais calculée). Toutes retirées et remplacées par des affirmations honnêtes sur ce qui est réellement garanti (accès RLS réservé aux membres de la conversation) — règle anti-fausse-promesse explicite de la spécification messagerie. `docs/ETAT_ACTUEL.md`, `docs/modules/05_reseau_mok_et_social.md` et `docs/supabase_schema.sql` (obsolète, prédate la vraie migration) corrigés en conséquence — ils décrivaient encore `chat_conversations`/`chat_messages`, un chiffrement de bout en bout, un partage d'écran WebRTC et un épinglage fonctionnel, aucun n'existant réellement. **Explicitement différé** : recherche plein texte dans l'historique des messages (aucun index GIN/tsvector n'existe sur `messages.content`, confirmé par lecture directe — laissé pour le futur moteur de recherche universelle, LOOP 10-11/17, plutôt qu'un second moteur de recherche propre à la messagerie) ; handoff réel Conversation↔Live (aucun des deux sens n'existe aujourd'hui — dépend de l'UI de la mission LIVE séparée, hors périmètre d'une seule LOOP) ; transport audio/vidéo réel entre deux participants d'un appel (la signalisation ringing/accept/reject est réelle, mais `ChatCallModal.tsx` ne capture que la caméra locale — aucun `RTCPeerConnection`, donc aucun média n'est réellement échangé ; corriger ceci nécessiterait soit réparer `services/webrtcService.ts` — déjà qualifié de code mort par l'audit de la mission LIVE — soit étendre l'adaptateur LiveKit de cette même mission aux appels 1-1, les deux hors périmètre d'une LOOP messagerie). |
| **Dossiers de vie** | `dossiers`, `dossier_steps`, `dossier_tasks`, `dossier_documents`, `dossier_deliverables`, `dossier_appointments`, `dossier_shares` | Domaine le mieux justifié par l'audit (CRUD réel préexistant). Partage explicite via `dossier_shares`. |
| **Carrière** | `career_goals`, `career_opportunities`, `career_opportunity_feedback`, `career_search_missions`, `master_resumes`, `career_snapshots` | Seuls Radar + CV Maître avaient un CRUD prouvé. `career_snapshots` (jsonb) reçoit les ~70 autres types Carrière (Boussole, simulations, journal...) sans normalisation forcée. |
| **Éducation / Campus** | `courses`, `enrollments`, `exam_sessions`, `certificates` | `enrollments` UNIQUE(user_id, course_id). Corrige un bug d'index de l'ancien code (`cloud.ts` indexait les certificats sur `studentName` au lieu d'un vrai `user_id`). |
| **Commerce (minimal)** | `shops`, `products`, `orders`, `order_items` | `orders`/`order_items` n'existaient pas du tout côté code — nécessaires pour que le panier devienne réel. |
| **Finance** | `wallet_transactions` | Solde **dérivé** (`get_wallet_balance()`), jamais stocké — évite toute désynchronisation. Écriture uniquement via `insert_wallet_transaction()` (RPC). |
| **Notifications** | `notifications` | Realtime activé — mais confirmé non consommé côté client jusqu'au LOOP 08/17 (voir ci-dessous), malgré l'affirmation de cette ligne. **LOOP 08/17 (mission Architecte MOCnet, moteur de notifications : fondation, 29 août 2026)** — audit direct (`pg_proc`/`pg_trigger`) : seule `notify_friendship_event()` (mission précédente) générait des notifications réelles ; aucun trigger équivalent sur `messages`/`comments`/`follows`. Ajoutés au même patron (`SECURITY DEFINER`, `EXECUTE` révoqué pour `public`/`anon`/`authenticated` dès l'origine — ces fonctions ne sont destinées qu'au déclenchement par trigger) : `notify_message_event()` (notifie chaque AUTRE participant d'une conversation, priorité `'normal'`), `notify_comment_event()` (notifie l'auteur du post, jamais soi-même, priorité `'normal'`), `notify_follow_event()` (notifie la personne suivie, priorité `'low'`) — les réactions sont **volontairement exclues** (« un like n'a pas besoin d'un accusé », principe explicite de la spécification du moteur de notifications). Réalité corrigée côté client : `subscribeToNotifications()` (Realtime `postgres_changes`, même patron que `subscribeToChat`) enfin branché dans `GlobalContext.tsx` — jusqu'ici seul un fetch ponctuel au montage/à l'authentification existait, une notification créée pendant que l'app était déjà ouverte n'apparaissait qu'au rechargement suivant. `priority`/`target_action` (colonnes réelles depuis l'origine) enfin mappées côté client (`types.ts::Notification`), `priority='high'` affiché comme badge « Important » dans les deux panneaux de notifications (desktop et mobile, `Layout.tsx`) — priorité fixée par le TYPE d'événement, jamais par l'émetteur. **Découverte critique en testant la diffusion admin** : `adminConfigService.sendBroadcastNotification` n'écrivait que dans un tableau en mémoire (`localStorage`) — un admin croyait diffuser une alerte à toute la communauté, mais aucun autre utilisateur ne recevait jamais rien ; corrigé par `broadcastNotification()` (fan-out réel dans la même table `notifications`, `notifications_owner` autorisant déjà un admin — `is_admin()` — à écrire pour n'importe quel `user_id`, aucune fonction `SECURITY DEFINER` nécessaire ici). **Second problème trouvé en testant ce correctif, avant tout commit** : `BroadcastNotification.targetAudience` (`'citizens'`/`'partners'`) hérite du vocabulaire de rôles fictif d'`AdminConfigService` (`'citizen'/'partner'/'guest'`), jamais réconcilié avec la vraie contrainte `profiles_role_check` (valeurs réelles : `user`/`admin`/`expert`/`mentor`/`moderator`/`organization`/`super_admin` — **aucune ligne n'a jamais `role='citizen'` ou `role='partner'`**, un filtre littéral sur ces valeurs aurait toujours matché zéro destinataire) ; mappé vers l'équivalent réel le plus proche (`user`→« citoyens », `organization`→« partenaires ») — réconcilier tout le vocabulaire de rôles d'`AdminConfigService` avec le schéma réel est un chantier à part entière, hors périmètre d'une LOOP notifications, signalé ici pour ne pas disparaître silencieusement. Vérifié par tests réels (impersonation SQL pour les 3 triggers avec anti-self-notification, appels REST réels avec JWT admin pour la diffusion — y compris la vérification négative qu'un compte non-admin ne peut pas écrire une notification pour un tiers, `42501` confirmé). **Explicitement différé** : regroupement intelligent de notifications similaires (ex. « 3 nouveaux commentaires » au lieu de 3 lignes — la table ne porte aucune clé de regroupement, et le principe « ne jamais fusionner des décisions distinctes » de cette LOOP est trivialement respecté en gardant une ligne par événement pour l'instant), navigation réelle au clic sur `target_action` (le mapping existe côté données mais aucun composant ne l'exploite encore pour rediriger), notifications push navigateur/mobile (aucune infrastructure, `Notification.requestPermission`/`PushManager` absents de tout le dépôt), préférences de notification (filtrage par catégorie — mode silencieux livré au LOOP 09/17, voir ci-dessous) — prévus au-delà de cette mission. **LOOP 09/17 (Notifications/Attention : orchestration proactive, 29 août 2026)** — table `reminders` (RLS owner-only + `is_admin()` non nécessaire ici, index partiel `where status='pending'`) + fonction `fire_due_reminders()` (`SECURITY DEFINER`, `EXECUTE` révoqué pour `anon`/`authenticated` dès l'origine) transformant un rappel dû en notification réelle **une seule fois** (`pending`→`fired`, jamais récurrent sauf nouveau rappel créé explicitement), orchestrée par un job `pg_cron` réel (`fire-due-reminders`, toutes les 5 minutes — `pg_cron` v1.6.4 confirmé installé, aucun job préexistant avant cette LOOP). Digest quotidien honnête : `send_daily_digest()` (`pg_cron`, quotidien 8h UTC) agrège en UNE notification les non-lues réellement accumulées depuis le dernier digest de chaque utilisateur (fenêtre calculée par sous-requête corrélée, jamais de double-comptage) et **n'envoie rien si le compte est nul** — aucun « rien de neuf » fabriqué. Mode silencieux réel : `privacySettings.notificationsMuted` (jsonb existant, aucune migration nécessaire) éteint uniquement le point rouge du clic sur la cloche (desktop + mobile, `Layout.tsx`) — jamais les notifications elles-mêmes, qui restent pleinement visibles dans le panneau (« réduire l'interruption, jamais l'information »), branché pour la première fois sur un `onUpdateProfile` réellement câblé jusqu'à `<UnifiedSettingsModal>` (jusqu'ici absent de son seul point de montage, `Layout.tsx` — le bouton « Enregistrer » du modal était inerte pour tout ce qu'il affiche). Multi-appareils : `subscribeToNotifications()` écoute désormais aussi `event: 'UPDATE'` (pas seulement `INSERT`), propageant en direct un changement de statut `read` fait sur un autre appareil déjà ouvert. Aucune UI de création de rappel dans cette LOOP (décision explicite de périmètre — testé directement via SQL/REST avec de vrais jetons JWT, y compris les cas négatifs : Alice ne peut ni lire ni créer de rappel pour Bob, `42501` confirmé). `get_advisors` revérifié après les deux migrations de cette LOOP : zéro nouvelle alerte `SECURITY DEFINER` exécutable (`EXECUTE` révoqué dès l'origine), seules les 2 alertes génériques `pg_graphql_*_table_exposed` attendues pour toute nouvelle table de ce schéma (même bruit de fond que les ~75 autres tables du projet, jamais traité comme actionnable dans cette mission). |
| **Fichiers** | `documents`, `document_shares` | Métadonnées uniquement ; fichiers réels dans Supabase Storage (§6). Digital Safe était un stub vide côté code (`cloudService.uploadFile` ne stockait rien) — première implémentation réelle. |
| **Live** | `live_sessions`, `live_speakers`, `live_attendance`, `live_questions` (+upvotes), `live_polls`(+options+votes), `live_agenda_items`, `live_decisions`, `live_action_items`, `live_documents`, `live_source_cards`, `live_products`, `live_personal_notes`, `gift_catalog`, `live_gifts_sent`, `live_replays`, `live_whiteboard_strokes`, `live_messages`, `live_reactions` | Couverture intégrale de `SocialLive.tsx`/`LiveStream` (pas une version réduite — décision explicite, voir §4). `live_messages`/`live_reactions` ajoutées au LOOP 02/14 (refonte LIVE) : chat libre et réactions ponctuelles, absentes du schéma d'origine (qui couvrait Q/R structurée via `live_questions`, pas le chat). Bug RLS corrigé au même LOOP : `live_sessions_select_visible` dupliquait à la main la logique de `can_view_live_session()` avec une faute de corrélation (`sp.id`/`a.id` au lieu de l'id de session), rendant invisible une session privée à ses propres speakers/attendees — la policy appelle maintenant directement la fonction (déjà correcte). Ces 18 tables restent, comme au départ, non consommées par aucun code applicatif avant le LOOP 02/14 (aucun `.from('live_...` dans le dépôt) — le branchement réel commence aux LOOPs suivants (03+). `live_sessions.visual_universe` ajoutée au LOOP 08/14 (5 univers visuels, prompt 3/7) : réglage de session modifiable par l'hôte uniquement, diffusé à tous via Realtime `postgres_changes` UPDATE — `live_sessions` a dû être ajoutée à la publication `supabase_realtime` à cette occasion (elle ne l'était pas, contrairement à `live_messages`/`live_reactions`/`live_speakers` déjà ajoutées au LOOP 05/14). |
| **Live Solidaire** | `live_solidarity_causes`, `live_solidarity_wallet_ledger`, `live_solidarity_proofs`, `live_solidarity_updates`, `live_solidarity_donors` | Complément reçu pendant LOOP 05/14, intégré aux LOOPs restantes (pas une mission séparée). Schéma migré au LOOP 09/14 car la création vocale d'une mission (`live_solidarity_causes`, consommée par `services/live/liveSolidarityService.ts`) en avait besoin en premier ; les 4 autres tables n'ont pas encore de consommateur (arrivera aux LOOPs 11/12 — preuves via vision, permissions/confidentialité), RLS déjà posée (visibilité alignée sur `can_view_live_session()` de la session d'origine, écriture réservée à l'organisateur/admin). Ledger append-only, jamais un solde stocké (même principe que `wallet_transactions`) — aucune de ces tables ne détient de vrais fonds : le mouvement réel d'argent reste `INTÉGRATION EXTERNE REQUISE` (prestataire de paiement), hors périmètre de ce sandbox. |
| **Agents** | `agents` | Catalogue des 13 experts Diallo. Admin-managé, lecture publique. |

**Standards transverses** : `created_at`/`updated_at` (trigger `set_updated_at`), UUID (`gen_random_uuid()`), index sur toutes les FK, contraintes UNIQUE anti-doublons (réactions, participants, inscriptions, votes).

---

## 3. Rôles et permissions

`profiles.role` ∈ `user | admin | expert | mentor | moderator | organization | super_admin` (contrainte `check`). Seuls `user`/`admin` sont utilisés aujourd'hui ; les autres sont préparés pour les évolutions futures sans migration supplémentaire.

- **Bootstrap admin** : le trigger `handle_new_user()` attribue `role = 'admin'` uniquement si l'email correspond à l'admin historique (`visionsmart224@gmail.com`), sinon `'user'`. Cette logique vit **côté base**, plus jamais côté client.
- **Vérification** : fonction `is_admin()` (SECURITY DEFINER, lit `profiles.role` du user courant) — utilisée dans toutes les policies nécessitant un accès admin.
- **Colonnes protégées** : `role`, `credits`, `xp`, `level`, `next_level_xp` sur `profiles` ne peuvent être modifiées ni par un `UPDATE` client direct, ni par une policy RLS "own row" — un trigger `protect_profile_sensitive_columns` les restaure à leur valeur précédente sauf appel `service_role` ou RPC de confiance (voir `award_xp_and_credits`).

---

## 4. Décisions de périmètre (traçabilité)

1. **Auth + domaines déjà réels uniquement** (validé explicitement) : pas de normalisation de Trade/Commerce Mondial/MokTrust (0% persistance prouvée).
2. **Live intégral** (ajusté suite à une demande explicite en cours de mission) : contrairement au choix initial "minimal", tout `SocialLive.tsx` est couvert (questions, sondages, agenda, décisions, documents, cadeaux, présence, tableau blanc) — rien de ce qui existe déjà dans l'UI n'est laissé de côté.
3. **Tribus/Cercles** : hors périmètre (quasi aucune structure réelle dans le code — 1 tribu mock, `members` est un entier, pas une relation).

---

## 5. Fonctions RPC clés

| Fonction | Rôle |
|---|---|
| `is_admin()` | Vérifie si l'utilisateur courant est admin (utilisée dans les policies). |
| `can_view_live_session(id)` / `is_live_host(id)` | Visibilité et droits d'écriture sur une session Live. |
| `can_access_dossier(id)` / `can_write_dossier(id)` | Visibilité et droits d'écriture sur un dossier (propriétaire ou partage explicite). |
| `get_wallet_balance(user_id, currency)` | Solde dérivé (somme des transactions), avec vérification d'autorisation (soi-même ou admin). |
| `insert_wallet_transaction(...)` | Seule voie d'écriture dans `wallet_transactions` — vérifie le solde avant un débit. |
| `award_xp_and_credits(user_id, xp_delta, credits_delta)` | Seule voie légitime pour modifier `credits`/`xp`/`level` sur `profiles`. |

---

## 6. Storage

Deux buckets (pas un par écran) :
- **`public`** — avatars, images de posts/stories. Lecture publique, écriture restreinte au chemin `{domaine}/{user_id}/...` du propriétaire.
- **`private`** — documents, CV, pièces Carrière/Campus. Aucune URL publique permanente ; accès uniquement au propriétaire (policy Storage), le partage explicite (`document_shares`/`dossier_shares`) se fait par **signed URLs** générées côté app, jamais en élargissant la policy du bucket.

---

## 7. Realtime

Activé uniquement là où il apporte une vraie valeur (mission section 26) : `messages`, `notifications`, `live_questions`, `live_poll_votes`, `live_whiteboard_strokes`. Pas de Realtime généralisé à toutes les tables.

---

## 8. État des lieux avant/après

Avant cette mission : **aucun backend réel**. Authentification 100% simulée (mot de passe jamais vérifié, session falsifiable via `localStorage`), toutes les données en `localStorage`/IndexedDB/state React, un seul projet Supabase existant avec une table stub vide (`mok`, supprimée lors de cette migration).

Voir `docs/AUTHENTICATION.md` pour le détail du flux d'authentification et `docs/JOURNAL_DECISIONS.md` pour l'historique des décisions.

---

## 9. Orchestrateur central des modèles IA (Super Admin)

*Ajouté le 28 août 2026.* Écran Super Admin (`components/admin/AiOrchestrator.tsx`) pour enregistrer, configurer et activer des fournisseurs IA (LLM, voix, image/vidéo), avec un point d'entrée unique côté serveur (`ai-gateway`, Edge Function) qui sélectionne automatiquement le meilleur fournisseur actif par catégorie et **bascule sans interruption** vers le suivant en cas d'échec, d'indisponibilité ou de quota dépassé.

### Principe de sécurité

Les clés API ne transitent et ne sont **jamais lisibles côté client** : elles sont chiffrées dans **Supabase Vault** (`supabase_vault`, déjà installé sur ce projet), écrites uniquement via une RPC admin-only, et déchiffrées uniquement à l'intérieur de `ai-gateway` (rôle `service_role`). Le navigateur ne voit jamais une clé — seulement un indice (4 derniers caractères) et le statut du dernier test de connexion. C'est un changement délibéré par rapport au seul précédent existant : la clé Gemini reste, pour l'instant, injectée dans le bundle client via `vite.config.ts` (voir §12) — un choix qu'il ne fallait surtout pas reproduire à l'échelle de 35 fournisseurs.

### Tables

| Table | Rôle |
|---|---|
| `ai_providers` | Catalogue des fournisseurs (id, catégorie, `adapter_kind`, `base_url`, `priority`, `status`). Aucun secret. Lecture admin uniquement. |
| `ai_models` | Modèles disponibles par fournisseur, avec un modèle par défaut (`is_default`). |
| `ai_provider_credentials` | Pointeur vers le secret Vault (`vault_secret_id`) + indice de clé + état d'activation/test. RLS activé, **aucune policy cliente** (accès exclusivement via RPC). |
| `ai_call_log` | Journal de chaque tentative d'appel (fournisseur, catégorie, tentative n°, succès/échec, classe d'erreur, latence) — permet d'observer la bascule automatique. Lecture admin uniquement. |

### RPC

| Fonction | Rôle |
|---|---|
| `set_ai_provider_secret(provider_id, secret)` | Crée ou fait tourner la clé d'un fournisseur (Vault). Ne renvoie jamais le secret. Admin uniquement. |
| `set_ai_provider_enabled(provider_id, enabled)` | Active/désactive un fournisseur pour la sélection automatique. Admin uniquement. |
| `set_ai_provider_priority(provider_id, priority)` | Ordre de bascule au sein d'une catégorie (plus petit = essayé en premier). Admin uniquement. |
| `get_ai_provider_status()` | Statut de tous les fournisseurs (sans le secret) — alimente l'écran admin. Admin uniquement. |
| `get_ai_provider_secret_internal(provider_id)` | Lit le secret déchiffré. **Révoqué pour `anon`/`authenticated`**, accessible uniquement au rôle `service_role` (appelé uniquement par `ai-gateway`). |

Les 4 premières fonctions vérifient `is_admin()` en interne **et** ont leurs droits `EXECUTE` explicitement restreints à `authenticated` (jamais `anon`) — défense en profondeur, conforme à `get_advisors` (0 alerte restante après durcissement).

### Edge Function `ai-gateway`

Un seul point d'entrée (`supabase/functions/ai-gateway/`), pas une fonction par fournisseur : un registre d'adaptateurs en code (`adapters/registry.ts`) route par `adapter_kind`, pas par fournisseur individuel — plusieurs fournisseurs partagent un même adaptateur (`openai_compatible` sert 10 fournisseurs LLM différents, distingués uniquement par `base_url`).

Sélection automatique + bascule : la fonction lit les fournisseurs actifs+configurés d'une catégorie triés par `priority`, tente chacun dans l'ordre, journalise chaque tentative dans `ai_call_log`, et retourne le premier succès. Ajouter le fournisseur n°36 plus tard = une ligne `ai_providers` + éventuellement un seul nouveau fichier d'adaptateur, sans toucher au reste.

### Catalogue (35 fournisseurs demandés, 13 actifs)

**Actifs et réellement implémentés** — LLM : OpenAI, Anthropic (Claude), Google Gemini, DeepSeek, Qwen (DashScope), Kimi (Moonshot), Mistral, Grok (xAI), OpenRouter, Together AI, Fireworks AI, Cerebras (10 sur l'adaptateur `openai_compatible` + Claude et Gemini sur leur adaptateur dédié) ; Voix : ElevenLabs.

**Enregistrés, non configurés** (`status='not_implemented'`, emplacement prêt dans l'écran admin, adaptateur à ajouter plus tard sans redéploiement du reste) — LLM : Hugging Face, Replicate, SambaNova, DashScope (natif) ; Voix : Cartesia, PlayHT, Azure Speech, Google TTS, Amazon Polly, AssemblyAI, Deepgram, Whisper ; Image/vidéo : Flux, Ideogram, Recraft, Leonardo, Runway, HeyGen, Kling, Pika, Luma, Veo.

### Vérification effectuée

Round-trip Vault confirmé, `get_ai_provider_secret_internal` inaccessible en `authenticated`/`anon` (confirmé via `has_function_privilege`), `ai_provider_credentials` sans policy cliente (0 policy, RLS activé = deny-by-default), `get_advisors` re-vérifié après durcissement des `GRANT`. Aucune clé réelle n'a été saisie par l'agent — l'admin les ajoute lui-même via l'écran, un test de connexion par fournisseur reste donc à faire dès qu'une clé est renseignée (seul le round-trip technique a été vérifié côté agent).

### Hors périmètre (documenté, non fait)

Les ~40 appels directs à `GoogleGenAI` dans les composants existants (Carrière, Santé, Assistant IA des posts...) restent inchangés — ils continuent d'utiliser la clé Gemini injectée au build comme avant. Les rebrancher sur `ai-gateway` est un chantier séparé, à plus haut risque de régression, non traité dans cette mission.
